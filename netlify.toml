[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# GROQ_API_KEY is set in the Netlify UI (Site settings → Environment variables),
# never committed to this repo. Free key, no billing required: console.groq.com/keys
